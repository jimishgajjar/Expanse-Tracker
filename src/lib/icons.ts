// Default icons + the icon shown for deleted categories. The pickable icon list
// lives in components/icon.tsx (ICON_NAMES, derived from the render registry so
// the two can never drift).
export const DEFAULT_ACCOUNT_ICON = "wallet";
export const DEFAULT_CATEGORY_ICON = "tag";

// Icon shown for transactions whose category was deleted.
export const UNCATEGORIZED_ICON = "circle-help";
