# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# local MCP server config — may carry API keys; never commit (repo is public)
.mcp.json
.claude.json

# local embedded postgres (PGlite dev fallback) + dev server log
/.pglite
dev.log

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editors / OS junk
.idea/
.vscode/
*.swp
*~
Thumbs.db
Desktop.ini

# personal one-off scripts (local data import + email test — keep out of the repo)
/scripts/seed-jimish.ts
/scripts/seed-hani.ts
/scripts/seed-notion.ts
/scripts/test-email.ts
/scripts/copy-neon-to-local.ts

# agent design skills (local tooling — reinstall via `npx skills` / `npx impeccable`)
/.agents/
/.claude/skills/
/.impeccable/
/skills-lock.json
